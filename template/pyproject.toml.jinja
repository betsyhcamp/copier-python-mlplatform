{%- set py_mm = python_version.split('.')[:2] | join('.') -%}
{%- set py_target = 'py' + python_version.split('.')[:2] | join('') -%}
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "{{ package_name }}"
version = "0.1.0"
description = ""
authors = [
    { name = "{{ author_name }}", email = "{{ author_email }}" }
]
readme = "README.md"
requires-python = ">={{ py_mm }}"
dependencies = []

[dependency-groups]
dev = [
    "ruff",
    "pytest",
    "pre-commit",
    "jupytext",
]

[tool.hatch.build.targets.wheel]
packages = ["src/{{ package_name }}"]

[tool.ruff]
target-version = "{{ py_target }}"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
