name: Template CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    name: check (${{ matrix.project_type }}, ${{ matrix.ci_provider }}, ${{ matrix.python_version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        project_type: [base, package, pipeline-kfp]
        ci_provider: [none, github]
        python_version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python_version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install Task
        uses: arduino/setup-task@v2
        with:
          version: 3.x

      - name: Install copier
        run: uv tool install copier

      - name: Render template
        # --vcs-ref HEAD renders from the current commit rather than the latest
        # tag; without it copier would always test the last released version.
        run: |
          copier copy . ./generated \
            --vcs-ref HEAD \
            --defaults \
            --data project_name="test-project" \
            --data package_name="test_project" \
            --data python_version="${{ matrix.python_version }}" \
            --data author_name="CI" \
            --data author_email="ci@example.com" \
            --data project_type="${{ matrix.project_type }}" \
            --data ci_provider="${{ matrix.ci_provider }}"

      - name: Initialize project
        working-directory: ./generated
        # git init + commit required before any task runs: the pipeline-kfp
        # Taskfile evaluates `git rev-parse --short HEAD` at startup for its
        # Docker task vars, which fails on a repo with no commits.
        run: |
          git init
          git config user.email "ci@example.com"
          git config user.name "CI"
          git add -A
          git commit -m "Initial commit"
          uv sync

      - name: Run pre-commit
        working-directory: ./generated
        run: task pre-commit

      - name: Run tests
        working-directory: ./generated
        run: task test

      - name: Build docs
        if: matrix.project_type == 'package' || matrix.project_type == 'pipeline-kfp'
        working-directory: ./generated
        run: task docs

      - name: Build package
        if: matrix.project_type == 'package'
        working-directory: ./generated
        run: task build
